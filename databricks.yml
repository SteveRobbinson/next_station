bundle:
  name: geo-etl-package

artifacts:
  default:
    type: whl
    path: .

variables:
  volume_path:
    description: "Target path for python wheel"

targets:
  prod:
    workspace:
      root_path: ${var.volume_path}/${bundle.name}/${bundle.version}
